<?xml version="1.0" encoding="utf-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

	<!-- ===== Platform-specific shadows (Android needs much weaker shadow) ===== -->
	<Shadow x:Key="ActionGlow.WinUI"
            Brush="#204C9EFF"
            Opacity="0.35"
            Radius="40"
            Offset="0,14" />

	<Shadow x:Key="ActionGlow.Android"
            Brush="#303B5BDC"
            Opacity="0.12"
            Radius="16"
            Offset="0,6" />

	<!-- ===== Brushes ===== -->

	<!-- Normal: many small steps to avoid banding on Android -->
	<RadialGradientBrush x:Key="ActionNormalBrush">
		<!-- center glow -->
		<GradientStop Color="#E7FBFF" Offset="0.00" />
		<!-- very soft -->
		<GradientStop Color="#D8F8FF" Offset="0.06" />
		<GradientStop Color="#C4F1FF" Offset="0.12" />
		<!-- inner body -->
		<GradientStop Color="#A3E6FE" Offset="0.20" />
		<GradientStop Color="#86D7FA" Offset="0.30" />
		<GradientStop Color="#5FB9F2" Offset="0.42" />
		<GradientStop Color="#388FE6" Offset="0.55" />
		<!-- outer body -->
		<GradientStop Color="#246FDB" Offset="0.68" />
		<GradientStop Color="#1856BE" Offset="0.80" />
		<GradientStop Color="#123F98" Offset="0.90" />
		<GradientStop Color="#0D2F79" Offset="0.96" />
		<!-- fade to page background to kill edge band -->
		<GradientStop Color="{StaticResource BackgroundColor}" Offset="1.00" />
	</RadialGradientBrush>

	<!-- Pressed: same distribution, slightly darker/colder -->
	<RadialGradientBrush x:Key="ActionPressedBrush">
		<GradientStop Color="#D9F6FF" Offset="0.00" />
		<GradientStop Color="#C6EEFF" Offset="0.06" />
		<GradientStop Color="#B1E6FF" Offset="0.12" />
		<GradientStop Color="#8FD6FD" Offset="0.20" />
		<GradientStop Color="#73C6F7" Offset="0.30" />
		<GradientStop Color="#4FA8ED" Offset="0.42" />
		<GradientStop Color="#2F82E0" Offset="0.55" />
		<GradientStop Color="#2267C7" Offset="0.68" />
		<GradientStop Color="#184FA7" Offset="0.80" />
		<GradientStop Color="#123D88" Offset="0.90" />
		<GradientStop Color="#0C2E6C" Offset="0.96" />
		<GradientStop Color="{StaticResource BackgroundColor}" Offset="1.00" />
	</RadialGradientBrush>

	<!-- Disabled: more steps, gentle falloff -->
	<RadialGradientBrush x:Key="ActionDisabledBrush">
		<GradientStop Color="#EFEFEF" Offset="0.00" />
		<GradientStop Color="#E3E3E3" Offset="0.08" />
		<GradientStop Color="#D6D6D6" Offset="0.16" />
		<GradientStop Color="#C8C8C8" Offset="0.28" />
		<GradientStop Color="#B8B8B8" Offset="0.40" />
		<GradientStop Color="#9F9F9F" Offset="0.55" />
		<GradientStop Color="#777777" Offset="0.72" />
		<GradientStop Color="#545454" Offset="0.86" />
		<GradientStop Color="#3A3A3A" Offset="0.95" />
		<GradientStop Color="{StaticResource BackgroundColor}" Offset="1.00" />
	</RadialGradientBrush>


	<!-- ===== Style for Button ===== -->
	<Style x:Key="ActionButtonStyle" TargetType="Button">
		<!-- +20% size -->
		<Setter Property="WidthRequest" Value="240" />
		<Setter Property="HeightRequest" Value="240" />
		<Setter Property="CornerRadius" Value="120" />
		<Setter Property="HorizontalOptions" Value="Center" />

		<!-- Use brush for background -->
		<Setter Property="Background" Value="{StaticResource ActionNormalBrush}" />
		<Setter Property="BackgroundColor" Value="Transparent" />

		<!-- Border: use integer thickness on Android to avoid half-pixel AA -->
		<Setter Property="BorderColor" Value="#66FFFFFF" />
		<Setter Property="BorderWidth" Value="{OnPlatform Android=2, iOS=1.5, WinUI=1.5}" />

		<!-- Shadow: platform-specific -->
		<Setter Property="Shadow"
                Value="{OnPlatform Android={StaticResource ActionGlow.Android},
                                 iOS={StaticResource ActionGlow.WinUI},
                                 WinUI={StaticResource ActionGlow.WinUI}}" />

		<!-- Clip to a perfect circle so no gradient/antialias leaks past the edge -->
		<Setter Property="Clip">
			<Setter.Value>
				<!-- size is fixed 240x240 in this style -->
				<EllipseGeometry Center="120,120" RadiusX="120" RadiusY="120"/>
			</Setter.Value>
		</Setter>

		<!-- Visual states -->
		<Setter Property="VisualStateManager.VisualStateGroups">
			<Setter.Value>
				<VisualStateGroupList>
					<VisualStateGroup x:Name="CommonStates">
						<VisualState x:Name="Normal" />
						<VisualState x:Name="Pressed">
							<VisualState.Setters>
								<Setter Property="Background" Value="{StaticResource ActionPressedBrush}" />
								<Setter Property="Scale" Value="0.98" />
							</VisualState.Setters>
						</VisualState>
						<VisualState x:Name="Disabled">
							<VisualState.Setters>
								<Setter Property="Background" Value="{StaticResource ActionDisabledBrush}" />
								<Setter Property="Opacity" Value="0.85" />
							</VisualState.Setters>
						</VisualState>
					</VisualStateGroup>
				</VisualStateGroupList>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
