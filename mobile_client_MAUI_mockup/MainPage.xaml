<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiMockup.MainPage"
             BackgroundColor="{StaticResource BackgroundColor}">

    <!-- Root layout: shared tracks for header and content -->
    <Grid x:Name="RootLayout"
          RowDefinitions="10,Auto,*"
          ColumnDefinitions="Auto,*,Auto"
          ColumnSpacing="2"
          Padding="20"
          RowSpacing="40">

        <!-- spacer row -->
        <BoxView Grid.Row="0" Grid.ColumnSpan="3" BackgroundColor="Transparent" />

        <!-- Header placed directly in RootLayout columns (no inner Grid) -->
        <Button x:Name="LeftBtn"
                Grid.Row="1"
                Grid.Column="0"
                Text="&#x2039;"
                Clicked="OnLeftBtn"
                BackgroundColor="Transparent"
                BorderColor="Gray"
                BorderWidth="1"
                TextColor="White" />

        <Button x:Name="GateTitleButton"
                Grid.Row="1"
                Grid.Column="1"
                Clicked="OnTitleBtn"
                BackgroundColor="Transparent"
                BorderColor="Gray"
                BorderWidth="1"
                TextColor="White" />

        <Button Grid.Row="1"
                Grid.Column="2"
                Text="&#x203A;"
                Clicked="OnRightBtn"
                BackgroundColor="Transparent"
                BorderColor="Gray"
                BorderWidth="1"
                TextColor="White" />

        <!-- Content anchored to the same middle column as GateTitleButton -->
        <Grid x:Name="ContentGrid"
              Grid.Row="2"
              Grid.Column="1"
              RowDefinitions="Auto,*"
              RowSpacing="40">

            <!-- Status list -->
            <VerticalStackLayout x:Name="StatusLayout"
                                 Grid.Row="0"
                                 Spacing="12"
                                 HorizontalOptions="Start"
                                 VerticalOptions="Center">

                <!-- Gate status -->
                <Border Background="{StaticResource BackgroundColor}"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 8">
                    <!-- Fixed icon column + text column -->
                    <Grid Padding="10,8"
                          ColumnDefinitions="32,*"
                          ColumnSpacing="12">
                        <Path x:Name="GateStatusPath"
                              Data="{StaticResource GateIconGeometry}"
                              Fill="White"
                              WidthRequest="24"
                              HeightRequest="24"
                              Aspect="Uniform"
                              HorizontalOptions="Start"
                              VerticalOptions="Center" />
                        <Label Grid.Column="1"
                               x:Name="GateStatusLabel"
                               Text=""
                               TextColor="White"
                               FontSize="14"
                               HorizontalTextAlignment="Start"
                               VerticalTextAlignment="Center" />
                    </Grid>
                </Border>

                <!-- MQTT status -->
                <Border Background="{StaticResource BackgroundColor}"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 8">
                    <Grid Padding="10,8"
                          ColumnDefinitions="32,*"
                          ColumnSpacing="12">
                        <Path x:Name="MqttStatusPath"
                              Data="{StaticResource MqttIconGeometry}"
                              Fill="White"
                              WidthRequest="24"
                              HeightRequest="24"
                              Aspect="Uniform"
                              HorizontalOptions="Start"
                              VerticalOptions="Center" />
                        <Label Grid.Column="1"
                               x:Name="MqttStatusLabel"
                               Text="mqtt status"
                               TextColor="White"
                               FontSize="14"
                               HorizontalTextAlignment="Start"
                               VerticalTextAlignment="Center" />
                    </Grid>
                </Border>

                <!-- BT status -->
                <Border Background="{StaticResource BackgroundColor}"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 8">
                    <Grid Padding="10,8"
                          ColumnDefinitions="32,*"
                          ColumnSpacing="12">
                        <Path x:Name="BtStatusPath"
                              Data="{StaticResource BluetoothIconGeometry}"
                              Fill="White"
                              WidthRequest="24"
                              HeightRequest="24"
                              Aspect="Uniform"
                              HorizontalOptions="Start"
                              VerticalOptions="Center" />
                        <Label Grid.Column="1"
                               x:Name="BtStatusLabel"
                               Text="bt status"
                               TextColor="White"
                               FontSize="14"
                               HorizontalTextAlignment="Start"
                               VerticalTextAlignment="Center" />
                    </Grid>
                </Border>

            </VerticalStackLayout>

            <!-- Big action button centered within the middle column -->
            <Button x:Name="ActionButton"
                    Grid.Row="1"
                    HorizontalOptions="Center"
                    Style="{StaticResource ActionButtonStyle}"
                    Clicked="OnDoAction" />
        </Grid>
    </Grid>
</ContentPage>
