<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiMockup.MainPage"
             BackgroundColor="{StaticResource BackgroundColor}">
    <Grid>
        <!-- Portrait layout -->
        <Grid x:Name="PortraitLayout" IsVisible="True" RowDefinitions="Auto,*,Auto" Padding="20" RowSpacing="40">
            <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="2">
                <Button Text="&lt;" BackgroundColor="Transparent" BorderColor="Gray" BorderWidth="1" TextColor="White" />
                <Button Grid.Column="1" Text="Gate 1" BackgroundColor="Transparent" BorderColor="Gray" BorderWidth="1" TextColor="White" />
                <Button Grid.Column="2" Text="&gt;" BackgroundColor="Transparent" BorderColor="Gray" BorderWidth="1" TextColor="White" />
            </Grid>
            <Grid Grid.Row="1" RowDefinitions="*,2*">
                <VerticalStackLayout Grid.Row="0" Spacing="10" HorizontalOptions="Center" VerticalOptions="Center">
                    <Frame BackgroundColor="#00B470" CornerRadius="8" Padding="10,5" HasShadow="False">
                    <HorizontalStackLayout>
                        <Label Text="📶" FontSize="14" />
                        <Label Text="Wi-Fi Online" TextColor="White" FontSize="14" Margin="5,0,0,0" />
                    </HorizontalStackLayout>
                </Frame>
                <Frame BackgroundColor="#EB003F" CornerRadius="8" Padding="10,5" HasShadow="False">
                    <HorizontalStackLayout>
                        <Label Text="🔵" FontSize="14" />
                        <Label Text="BT Offline" TextColor="White" FontSize="14" Margin="5,0,0,0" />
                    </HorizontalStackLayout>
                </Frame>
                </VerticalStackLayout>
                <Button Grid.Row="2" HeightRequest="200" WidthRequest="200" CornerRadius="100" Clicked="OnDoAction" HorizontalOptions="Center">
                    <Button.Background>
                        <RadialGradientBrush>
                            <GradientStop Color="#00B8D4" Offset="0.2" />
                            <GradientStop Color="#1DE9B6" Offset="0.8" />
                        </RadialGradientBrush>
                    </Button.Background>
                </Button>
            </Grid>
        </Grid>

        <!-- Landscape layout -->
        <Grid x:Name="LandscapeLayout" IsVisible="False" RowDefinitions="Auto,*" Padding="20" RowSpacing="20">
            <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="2">
                <Button Text="&lt;" BackgroundColor="Transparent" BorderColor="Gray" BorderWidth="1" TextColor="White" />
                <Button Grid.Column="1" Text="Gate 1" BackgroundColor="Transparent" BorderColor="Gray" BorderWidth="1" TextColor="White" />
                <Button Grid.Column="2" Text="&gt;" BackgroundColor="Transparent" BorderColor="Gray" BorderWidth="1" TextColor="White" />
            </Grid>
            <Grid Grid.Row="1" ColumnDefinitions="Auto,*" ColumnSpacing="40" VerticalOptions="Center">
                <VerticalStackLayout Grid.Column="0" Spacing="10" VerticalOptions="Center">
                    <Frame BackgroundColor="#00B470" CornerRadius="8" Padding="10,5" HasShadow="False">
                        <HorizontalStackLayout>
                            <Label Text="📶" FontSize="14" />
                            <Label Text="Wi-Fi Online" TextColor="White" FontSize="14" Margin="5,0,0,0" />
                        </HorizontalStackLayout>
                    </Frame>
                    <Frame BackgroundColor="#EB003F" CornerRadius="8" Padding="10,5" HasShadow="False">
                        <HorizontalStackLayout>
                            <Label Text="🔵" FontSize="14" />
                            <Label Text="BT Offline" TextColor="White" FontSize="14" Margin="5,0,0,0" />
                        </HorizontalStackLayout>
                    </Frame>
                </VerticalStackLayout>
                <Button Grid.Column="1" HeightRequest="200" WidthRequest="200" CornerRadius="100" Clicked="OnDoAction" HorizontalOptions="Center">
                    <Button.Background>
                        <RadialGradientBrush>
                            <GradientStop Color="#00B8D4" Offset="0.2" />
                            <GradientStop Color="#1DE9B6" Offset="0.8" />
                        </RadialGradientBrush>
                    </Button.Background>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>
